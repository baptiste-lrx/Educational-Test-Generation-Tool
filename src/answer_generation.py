def generate_answers(questions, context):
    # Pour simplifier, on peut considérer que les réponses sont extraites du contexte
    answers = []
    for question in questions:
        # Implémente une logique pour trouver la réponse dans le contexte
        answers.append("Réponse exemple")
    return answers
